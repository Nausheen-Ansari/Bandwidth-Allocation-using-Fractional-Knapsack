# Bandwidth-Allocation-using-Fractional-Knapsack
This C project simulates an ISP allocating limited bandwidth. It uses the Fractional Knapsack greedy algorithm to optimally distribute bandwidth to users based on their demand and priority, aiming to maximize the total priority value served.
